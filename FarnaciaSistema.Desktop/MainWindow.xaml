<Window x:Class="FarmaciaSistema.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FarmaciaSistema.Desktop"
        mc:Ignorable="d"
        Title="Menú Principal - Sistema de Farmacia" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Menú Principal" 
                   Grid.ColumnSpan="3" 
                   HorizontalAlignment="Center" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Margin="20"/>

        <Button Grid.Row="1" Grid.Column="0" Content="Punto de Venta"/>
        <Button x:Name="BtnInventario" Grid.Row="1" Grid.Column="1" Content="Inventario" Click="BtnInventario_Click"/>
        <Button x:Name="Name" Grid.Row="1" Grid.Column="2" Content="Proveedores" Click="BtnProveedores_Click"/>

        <Button Grid.Row="2" Grid.Column="0" Content="Compras"/>
        <Button Grid.Row="2" Grid.Column="1" Content="Reportes"/>
        <Button Grid.Row="2" Grid.Column="2" Content="Usuarios"/>
    </Grid>
</Window>
